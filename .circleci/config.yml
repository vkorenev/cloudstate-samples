jobs:
  protoc:
    docker:
      - image: circleci/golang
    steps:
      - run: protoc --version
orbs:
  protobuf: izumin5210/protobuf@0.0.1
version: 2.1
workflows:
  build:
    jobs:
      - protobuf/install
      - protoc
